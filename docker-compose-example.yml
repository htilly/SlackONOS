services:
  slackonos:
    container_name: slackonos
    hostname: slackonos
    image: htilly/slackonos:latest
    restart: unless-stopped
    volumes:
      - PATH_TO_CONFIG_FOLDER/slackonos/config:/app/config
    ports:
      - "8080:8080"  # HTTP server for TTS (webPort from config)